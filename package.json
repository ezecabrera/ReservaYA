{
  "name": "reservaya",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "dev:app": "turbo run dev --filter=@reservaya/app",
    "dev:panel": "turbo run dev --filter=@reservaya/panel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean",
    "seed:demo": "node scripts/seed-demo.mjs",
    "seed:demo:reset": "node scripts/seed-demo-reset.mjs"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "turbo": "^2.3.3",
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
